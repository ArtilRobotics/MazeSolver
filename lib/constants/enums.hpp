#pragma once

enum COMMANDS
{
	L_Izau = 1,
	SETPOSITION = 2,
	RESETPOSITION = 3,
	SPEED_SLIDER = 888,
	AGARRE = 4,
	ANULAR_MENIQUE = 5,
	MEDIO = 6,

};

enum COMMAND_RESULT
{
	EMPTY,
	VALID,
	INVALID,
	NOT_CONNECTED
};

enum COMMAND_OPERATION
{
	INCREASE,
	DECREASE,
	TURN_ON,
	TURN_OFF,
	SET_DEFAULT,
	SET_SETPOINT
};

enum SLIDER_STATUS
{
	OFF,
	ON,
};